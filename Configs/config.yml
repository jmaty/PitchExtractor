log_dir: "Checkpoint"
save_freq: 10
epochs: 100 # (100)
batch_size: 512  # (64)
grad_accum_steps: 1
pretrained_model: ""
train_data: "Data/train_all.csv"
val_data: "Data/valid_all.csv"
# num_workers: 16  # 16
  
optimizer_params:
  lr: 0.0024 # (0.0003, 0.0005)
  pct_start: 0.0 # (0.0)
  
loss_params:
  lambda_f0: 0.1

mixed_precision: "bf16" # ("no")
